# Fragmented Building Upgrades Progress

- Task 1 complete: 1–10 level domain rules, N-fragment readiness/confirmation, normalization, six ten-level summaries, and temporary legacy visual fallback. Targeted 53/53 and full 314/314 tests passed; typecheck, lint, and format passed; review approved with no Critical or Important findings.
- Task 2 complete: shared `safeStorage`, `cityProgressMigration` (key `dobe-city-progression-v1`, normalization/migration), and persisted `useCityStore` with `buildingProgress` as the single source of truth plus `completeNextFragment`/`confirmBuildingLevelUp`; `useGangStore` migrated to the shared safe storage. Persist saves only `buildingProgress`; rehydrate/merge normalizes untrusted data and re-syncs a clearly-commented derived `buildingLevels` view. The temporary legacy `upgradeBuilding` action is a strict no-op for partial/ready progress and remains only for the old UI until Task 5. Full 340/340 tests, typecheck, lint, and format passed; review approved with no Critical or Important findings.
- Task 3 complete: six building kinds now expose ten stable, semantic fragment blueprints with local anchors, unique geometry signatures, current/target/scaffold partial states, opt-in target-only animation IDs, and bounded adjacent-level enhancement. Analytic rotated AABB tests cover every full and partial state without geometry allocation. Full 365/365 tests, typecheck, lint, and format passed; review approved with no Critical or Important findings.
- Task 4 complete: fragment types moved to `buildingVisualTypes.ts` and the legacy `buildingVisualConfig` / `getBuildingVisualStage` (+ test) deleted with zero remaining imports. `AnimatedBuildingFragment` uses a resettable controller for a 400ms anchored entrance (scale .78→1, y −0.35→0) and an isolated green point light, never mutating business materials; interruption, repeated use of the same stable ID, 400ms completion, and reduced motion all snap safely to rest. `BuildingModel` memoizes fragment selection, renders stable anchored groups and translucent scaffolds, and preserves highlight/clubhouse neon. `BuildingVisual` only animates same-level p→p+1 completion from this session, not rehydrate/refresh. Full 391/391 tests, typecheck, lint, and format passed; review approved with no Critical or Important findings.
- Task 5 complete: `BuildingPanel` now mirrors the reference Setup loop with a named progressbar, current facility card, N status cells, one-fragment actions, a separate ready confirmation, and Lv.10 max state. The newest completed cell receives a one-shot green pop while reduced motion disables all upgrade motion. Legacy `buildingLevels`, `upgradeBuilding`, and `upgradeBuildingLevel` APIs were removed so `buildingProgress` is the sole city source. Full 391/391 tests, typecheck, lint, and format passed; review approved with no Critical or Important findings.
- Task 6 complete: fresh format/typecheck/lint/build green and full 391/391 (32 files) tests passed; `dist/index.html` references the `/DobeDemo/` base. New repeatable safe-mode acceptance script `.superpowers/sdd/fragmented-upgrades-cdp.mjs` (port preflight, only kills self-started process trees, temp OS profile, non-zero on failure, pure-data assertion self-test) drove real Chrome/CDP: fresh storage → click 修车厂 = Lv.1/10, 0/2, named progressbar, 2 cells (lv1 screenshot); first fragment → 1/2 with a 350×350 building ROI (derived from the real 修车厂 click coordinate, clipped to the viewport, HUD/panel masked) changing 299 px / 0.296% (partial screenshot); reload persisted 1/2; second fragment → ready with main level still 1; confirm → Lv.2, 0/3; injected valid Lv.10 save → maxed UI, 10 cells, disabled button (lv10 screenshot); panel stays within bounds and non-overflowing at desktop 1440×900 and mobile 390×844. Human screenshot check confirms the built-up sub-buildings stay inside the plot and never overlap the roads. All 30 live assertions + self-test passed; not committed/pushed and Pages not updated. Docs (README, gang requirement, session ledger, plan) updated.
