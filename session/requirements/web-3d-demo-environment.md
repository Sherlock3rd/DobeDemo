# Web 3D Demo 工作环境

## 当前状态

已完成

## 已确认技术栈

- Vite：开发与生产构建
- React + TypeScript：应用外壳与 UI
- Three.js + React Three Fiber + Drei：3D 场景渲染
- Zustand：跨组件共享状态
- Vitest + React Testing Library：单元与 UI 行为测试
- ESLint + Prettier：代码质量与格式

## 范围

- 初始化 Vite、React 与 TypeScript 工程。
- 使用 React Three Fiber 构建 3D 场景，Drei 提供常用辅助能力。
- 使用 Zustand 管理确需跨模块共享的游戏状态。
- 建立代码质量、测试、构建与项目治理基础。
- 初始 Demo 提供透视相机、灯光、地面、可旋转示例物体、基础 HUD 与错误边界。
- 加载态基础设施已接入；初始场景无异步资源，因此正常启动时不触发。

## 非目标

- 不实现具体游戏机制。
- 不引入后端、账号系统、数据库或联网功能。
- 不制作正式模型、贴图、动画或音频。
- 不初始化或提交 Git 仓库。
- 不配置或授权飞书 CLI，不执行飞书文档或电子表格写入。

## 验收条件

1. [x] 依赖已正常安装。
2. [x] 开发服务器已明确绑定 `127.0.0.1`，实际 URL `http://127.0.0.1:5173/` 通过 HTTP 200 与入口 HTML 冒烟验证；验证后端口及进程均已释放。
3. [x] 浏览器应用已组装基础 3D 场景与覆盖式 HUD。
4. [x] `npm run typecheck` 通过（退出码 0）。
5. [x] `npm run lint` 通过（退出码 0）。
6. [x] `npm run format:check` 通过（退出码 0）。
7. [x] `npm test` 通过（4 个测试文件、8 项测试全部通过）。
8. [x] `npm run build` 通过（退出码 0，生成 HTML、CSS 与 JS 产物）。

## 交付边界

- 未初始化 Git，工作区不存在 `.git/`。
- 未配置或授权飞书 CLI。
- 未创建后端、账号、数据库或联网功能。

## 变更记录

| 日期 | 变更摘要 |
|------|----------|
| 2026-07-22 | 创建需求记录，状态设为「实施中」 |
| 2026-07-22 | 完成应用组装、响应式样式、全量自动化检查与 HTTP 冒烟，状态更新为「已完成」 |
| 2026-07-22 | 明确加载态基础设施行为：初始场景无异步资源，正常启动时不触发 |
