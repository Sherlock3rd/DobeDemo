# 工业城建筑互动升级 Demo

## 当前状态

已完成

## 参考图

- `example/cityview.jpg`：工业城整体俯视构图参考。
- `example/citystyle.jpg`：低多边形工业建筑视觉风格参考。

## 范围

- 六座可互动建筑：废车回收站、物流中心、加油站、修车厂、Clubhouse（保留英文名）、商业区。每座建筑各有互不重叠的合理 footprint、独立配色（主色 + 强调色）。
- 每座建筑三级外观与文字说明（1→2→3 级由简单向复杂演进，例如加油站从双泵岛演进到重型车辆加注区）。点击建筑弹出面板可升级，满 3 级后禁用升级按钮。
- 固定倾角的正交（orthographic）相机：仅支持拖拽平移与滚轮缩放，不支持旋转；平移范围与缩放范围受 `CAMERA_CONFIG` 约束。
- 城市静态场景：草地基底、混凝土地块、道路、河流、环境仓库、静态车辆、树木，均通过轴对齐包围盒（含旋转投影）互不重叠验证。
- 应用外壳：`AppErrorBoundary` 错误边界、Drei `Loader` 加载态、城市 HUD（标题 + 操作提示）、建筑面板（标题、等级 badge、说明、升级/关闭按钮）。
- 响应式桌面横屏控制台视觉：深色工业风背景、HUD 左上角面板、建筑面板桌面态右侧浮层／小屏（<720px）底部抽屉。

## 非目标

- 不实现除六座建筑升级外的具体游戏机制（无经济系统、无多人协作、无存档持久化）。
- 不引入后端、账号系统、数据库或联网功能。
- 不制作正式美术资源（模型、贴图、动画、音频），建筑与场景均为程序化几何体。
- 不支持相机旋转或自由视角。
- 不初始化或提交 Git 仓库。
- 不配置或授权飞书 CLI，不执行飞书文档或电子表格写入。

## 验收条件

1. [x] 六座建筑数据、目录与升级纯函数（`src/game`）测试通过。
2. [x] 静态城市布局比例、镜头边界约束与全量空间不重叠校验（含环境物体、道路、河流、车辆、树木）测试通过。
3. [x] `useCityStore` 选中/升级/清空选择/reset 行为测试通过。
4. [x] `BuildingPanel`、`CityHud` 展示与交互（含事件不外泄）测试通过。
5. [x] 六类建筑三级视觉配置与命中盒高度不变量测试通过。
6. [x] 城市地面、环境、镜头控制与场景组合（`CityScene`）已实现。
7. [x] `App.tsx` 已切换为正交相机 + `CityScene` + `CityHud` + `BuildingPanel`，不再引用旧 Demo；`App.test.tsx` 覆盖标题切换、正交相机 prop、预选建筑面板展示三项断言。
8. [x] 响应式桌面控制台样式（`App.css`）已实现：HUD/面板视觉层级、升级按钮完整交互态、小屏底部抽屉、`prefers-reduced-motion` 支持。
9. [x] 旧 Demo 文件（`DemoScene`、`RotatingCube`、`useDemoStore`、`DemoHud` 及其测试）已删除，代码库无残留引用。
10. [x] `npm run format`／`typecheck`／`lint`／`format:check`／`test`／`build` 全部退出码 0；测试共 10 个文件、61 项全部通过。
11. [x] 最终开发服务器 HTTP 冒烟：`http://127.0.0.1:5175/` 返回 200，HTML 含 `#root` 与 `/src/main.tsx`；验证后完整进程树已终止，端口已释放。
12. [x] 已使用本机 Chrome 无头模式截取桌面截图至 `.superpowers/sdd/city-demo-screenshot.png`，视觉确认城市场景、HUD 与图标级渲染符合预期。

## 交付边界

- 未初始化 Git，工作区不存在 `.git/`。
- 未配置或授权飞书 CLI。
- 未创建后端、账号、数据库或联网功能。

## 变更记录

| 日期 | 变更摘要 |
|------|----------|
| 2026-07-22 | 创建需求记录，状态设为「已完成」，登记七个子任务的完整交付范围、验收条件与交付边界 |
